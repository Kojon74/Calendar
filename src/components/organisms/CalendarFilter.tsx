import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const CalendarFilter = (props: Props) => {
  return (
    <View>
      <Text>CalendarFilter</Text>
    </View>
  );
};

export default CalendarFilter;

const styles = StyleSheet.create({});
